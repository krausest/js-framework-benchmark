import benchmarkHelpers from 'ember-temp/utils/benchmark-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | benchmark-helpers', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = benchmarkHelpers();
    assert.ok(result);
  });
});
