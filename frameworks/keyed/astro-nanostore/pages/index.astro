<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Astro Nano Stores keyed</title>
  <link href="/css/currentStyle.css" rel="stylesheet" />
</head>
<body>
  <div id="main">
    <div class="container">
      <div class="jumbotron">
        <div class="row">
          <div class="col-md-6">
            <h1>Astro Nano Stores keyed</h1>
          </div>
          <div class="col-md-6">
            <div class="row">
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="run">Create 1,000 rows</button>
              </div>
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="runlots">Create 10,000 rows</button>
              </div>
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="add">Append 1,000 rows</button>
              </div>
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="update">Update every 10th row</button>
              </div>
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="clear">Clear</button>
              </div>
              <div class="col-sm-6 smallpad">
                <button type="button" class="btn btn-primary btn-block" id="swaprows">Swap Rows</button>
              </div>
            </div>
          </div>
        </div>
      </div>
      <table class="table table-hover table-striped test-data">
        <tbody id="tbody"></tbody>
      </table>
      <span class="preloadicon glyphicon glyphicon-remove" aria-hidden="true"></span>
    </div>
  </div>
  <template id="trow">
    <tr>
      <td class="col-md-1"></td>
      <td class="col-md-4"><a></a></td>
      <td class="col-md-1"><a><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a></td>
      <td class="col-md-6"></td>
    </tr>
  </template>
  <script>
    import { atom } from 'nanostores';

    // --- store ---

    let nextId = 1;

    const adjectives = [
      'pretty', 'large', 'big', 'small', 'tall', 'short', 'long', 'handsome', 'plain', 'quaint',
      'clean', 'elegant', 'easy', 'angry', 'crazy', 'helpful', 'mushy', 'odd', 'unsightly', 'adorable',
      'important', 'inexpensive', 'cheap', 'expensive', 'fancy',
    ];
    const colours = [
      'red', 'yellow', 'blue', 'green', 'pink', 'brown', 'purple', 'brown', 'white', 'black', 'orange',
    ];
    const nouns = [
      'table', 'chair', 'house', 'bbq', 'desk', 'car', 'pony', 'cookie', 'sandwich', 'burger',
      'pizza', 'mouse', 'keyboard',
    ];

    const _random = (max) => Math.round(Math.random() * 1000) % max;
    const _label = () =>
      adjectives[_random(adjectives.length)] + ' ' +
      colours[_random(colours.length)] + ' ' +
      nouns[_random(nouns.length)];

    function buildData(count) {
      const data = [];
      for (let i = 0; i < count; i++) {
        data.push({ id: nextId++, label: _label() });
      }
      return data;
    }

    const rows = atom([]);
    const selectedId = atom(null);

    const run = () => { rows.set(buildData(1000)); selectedId.set(null); };
    const runLots = () => { rows.set(buildData(10000)); selectedId.set(null); };
    const add = () => rows.set([...rows.get(), ...buildData(1000)]);
    const update = () => {
      const current = rows.get().slice();
      for (let i = 0; i < current.length; i += 10) {
        current[i] = { id: current[i].id, label: current[i].label + ' !!!' };
      }
      rows.set(current);
    };
    const clear = () => { rows.set([]); selectedId.set(null); };
    const swapRows = () => {
      const current = rows.get().slice();
      if (current.length <= 998) return;
      const tmp = current[1];
      current[1] = current[998];
      current[998] = tmp;
      rows.set(current);
    };
    const removeById = (id) => {
      const current = rows.get();
      const idx = current.findIndex((d) => d.id === id);
      if (idx < 0) return;
      rows.set([...current.slice(0, idx), ...current.slice(idx + 1)]);
    };

    // --- view ---

    const TBODY = document.getElementById('tbody');
    const TABLE = document.querySelector('table');
    const TROW = document.getElementById('trow');

    rows.subscribe((items) => {
      TBODY.textContent = '';
      const fragment = document.createDocumentFragment();
      for (let i = 0; i < items.length; i++) {
        const tr = TROW.content.firstElementChild.cloneNode(true);
        tr.children[0].textContent = items[i].id;
        tr.children[1].firstChild.textContent = items[i].label;
        tr.$id = items[i].id;
        fragment.appendChild(tr);
      }
      TBODY.appendChild(fragment);
    });

    selectedId.subscribe((id) => {
      for (const tr of TBODY.children) {
        tr.className = tr.$id === id ? 'danger' : '';
      }
    });

    document.getElementById('run').onclick = run;
    document.getElementById('runlots').onclick = runLots;
    document.getElementById('add').onclick = add;
    document.getElementById('update').onclick = update;
    document.getElementById('clear').onclick = clear;
    document.getElementById('swaprows').onclick = swapRows;

    TBODY.onclick = (e) => {
      const t = e.target;
      const n = t.tagName;
      const r = t.closest('TR');
      e.stopPropagation();
      if (n === 'SPAN' || (n === 'A' && t.firstElementChild)) {
        removeById(r.$id);
      } else if (n === 'A') {
        selectedId.set(r.$id);
      }
    };
  </script>
</body>
</html>
