<div class="jumbotron">
    <div class="row">
        <div class="col-md-6">
            <h1>Ember v3.5.1 (Sparkles)</h1>
        </div>
        <div class="col-md-6">
            <div class="row">
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='run' onClick=(action create)}}
                      Create 1,000 rows
                    {{/bs-button}}
                </div>
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='runlots' onClick=(action runLots)}}
                      Create 10,000 rows
                    {{/bs-button}}
                </div>
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='add' onClick=(action add)}}
                      Append 1,000 rows
                    {{/bs-button}}
                </div>
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='update' onClick=(action update)}}
                      Update every 10th row
                    {{/bs-button}}
                </div>
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='clear' onClick=(action clear)}}
                      Clear
                    {{/bs-button}}
                </div>
                <div class="col-sm-6 smallpad">
                    {{#bs-button id='swaprows' onClick=(action swapRows)}}
                      Swap Rows
                    {{/bs-button}}
                </div>
            </div>
        </div>
    </div>
</div>

<table class="table table-hover table-striped test-data">
  <tbody>
    {{#each data key='id' as |item|}}

      <Row
        @item={{item}}
        @rowClass={{if (eq item.id selected) 'danger' 'x'}}
        @onSelect={{action select item.id}}
        @onRemove={{action remove item.id}}
      />

    {{/each}}
  </tbody>
</table>


<span class="preloadicon glyphicon glyphicon-remove" aria-hidden="true"></span>
